import 'package:flutter/material.dart';

class CreateHabitPPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Create a new habit')),
      body: Card(),
    );
  }
}